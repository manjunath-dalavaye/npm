import { PerimeterOfCuboid } from "@ilihub/shape";

const length = 10;
const width = 20;
const height = 30;

const perimeter = PerimeterOfCuboid(length, width, height);
console.log(perimeter); // 240
