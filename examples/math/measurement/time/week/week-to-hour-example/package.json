{
  "name": "week-to-hour-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "find . -type f -name \"*.js\" -not -path \"./node_modules/*\" -exec sh -c 'val=$(node \"{}\"); echo \"{} ==> $val\"' \\;"
  },
  "devDependencies": {
    "@ilihub/week-to-hour": "workspace:*"
  }
}
