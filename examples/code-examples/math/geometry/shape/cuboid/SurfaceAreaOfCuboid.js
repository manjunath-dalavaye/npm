import { SurfaceAreaOfCuboid } from "@ilihub/code";

const length = 10;
const width = 20;
const height = 30;

const surfaceArea = SurfaceAreaOfCuboid(length, width, height);
console.log(surfaceArea); // 2200
