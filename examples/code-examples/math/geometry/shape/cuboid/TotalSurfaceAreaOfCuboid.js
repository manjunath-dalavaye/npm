import { TotalSurfaceAreaOfCuboid } from "@ilihub/code";

const length = 10;
const width = 20;
const height = 30;

const totalSurfaceArea = TotalSurfaceAreaOfCuboid(length, width, height);
console.log(totalSurfaceArea); // 2200
